---
# vars file for docker
redhat_dependencies:
  - yum-utils
  - device-mapper-persistent-data
  - lvm2

debian_dependencies:
  - apt-transport-https
  - ca-certificates
  - curl
  - gnupg-agent
  - software-properties-common

docker_packages:
  - docker-ce
  - docker-ce-cli
  - containerd.io